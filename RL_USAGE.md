# 强化学习系统使用指南

## 快速开始

### 启用RL模式

在 `config.py` 中设置：
```python
cfg.use_rl = True  # 启用RL模式
```

或者在创建场景时：
```python
from engulf.config import Config
cfg = Config()
cfg.use_rl = True

scene = TopDownScene(screen, cfg)
```

### 当前状态

✅ **已完成**：
- RL模块全部实现（11个文件）
- 集成到scene_topdown.py
- CPU优化（无GPU需求）
- 基因-RL融合系统

⚠️ **待完善**：
- PPO算法的反向传播（当前为简化版本）
- 更完善的状态编码（部分历史信息未实现）
- 训练可视化工具

## 系统说明

### 架构特点

1. **全RL模式**：所有生物都是RL智能体
2. **基因融合**：
   - 基因提供初始网络偏置
   - 基因控制物理约束（速度、体型上限）
   - 基因影响奖励权重（个性化奖励）
3. **全局训练**：所有生物共享经验，集中训练，参数同步

### 状态空间（210维）

- **自身状态**：位置、速度、大小、年龄等（25维）
- **基因特征**：64种基因的关键特征（30维）
- **局部地形**：8方向采样，当前地形（60维）
- **附近生物**：最多5个最近生物的信息（40维）
- **全局环境**：季节、密度、资源/危险水平（15维）
- **历史信息**：轨迹、奖励历史（25维）
- **计算特征**：距离目标、探索价值等（15维）

### 动作空间（12维连续）

- **移动控制**：方向(x,y)、速度比例、角速度（4维）
- **行为权重**：探索/资源/安全/社交权重、耐心（5维）
- **高级决策**：风险承受、竞争性、适应速度（3维）

### 奖励系统

**即时奖励**：
- 生存：每帧 +0.1
- 资源：获得资源 × 20.0
- 探索：探索新地形 +5.0
- 繁殖：成功繁殖 × 100.0
- 安全：回避危险 +10.0，遭遇危险 -20.0
- 成长：成长 × 30.0

**Episode奖励**（生命周期结束时）：
- 寿命奖励：实际寿命/基因寿命 × 30.0
- 繁殖奖励：后代数量 × 存活率 × 20.0
- 资源积累：总消耗 × 0.5

**个性化权重**（基于基因）：
- 高好奇心 → 资源/探索奖励权重增加
- 高谨慎度 → 安全奖励权重增加
- 高繁殖力 → 繁殖奖励权重增加

## 训练流程

1. **经验收集**：所有RL生物的行为被收集到全局缓冲区
2. **批量训练**：每100个经验触发一次训练（批次大小64）
3. **参数同步**：每100步将训练好的参数同步到所有生物
4. **统计输出**：每500步打印训练统计

## 性能优化（CPU）

1. **轻量级网络**：使用NumPy，无深度学习框架依赖
2. **小批次训练**：批次大小64，避免内存问题
3. **延迟更新**：不是每步都训练，而是积累后批量训练
4. **经验缓冲区限制**：50,000条经验（而非100万+）

## 调试和监控

### 查看训练统计

训练统计会在控制台每500步打印一次：
```
[RL] Updates: 10, Buffer: 1250, AvgReward: 2.345
```

### 切换模式

可以通过修改 `cfg.use_rl` 在运行时切换（需要重启场景）：
- `False`：传统基因驱动模式
- `True`：全RL模式

## 注意事项

1. **初始表现**：RL生物初始时表现随机，需要时间学习
2. **训练时间**：CPU训练较慢，建议先观察是否正常运行
3. **内存使用**：经验缓冲区限制为50K，避免内存溢出
4. **兼容性**：RL模式与基因模式可以共存（通过`use_rl`标志）

## 下一步优化建议

1. **实现完整反向传播**：当前PPO更新是简化版本
2. **添加Adam优化器**：提升训练稳定性
3. **实现经验优先级**：重要经验优先训练
4. **添加可视化**：实时显示训练曲线
5. **超参数调优**：根据实际表现调整学习率等参数

## 故障排除

**问题**：导入错误 `ModuleNotFoundError: No module named 'engulf.rl'`
- **解决**：确保 `engulf/rl/__init__.py` 存在

**问题**：生物不动或行为异常
- **检查**：确认 `cfg.use_rl = True` 且 RL模块正确导入
- **检查**：查看控制台是否有训练统计输出

**问题**：性能慢
- **优化**：减少批次大小或增加训练间隔
- **优化**：减少状态维度（简化编码）


